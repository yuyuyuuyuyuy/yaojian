# 药鉴 · 决策记录

> **用途**：本项目所有拍板的技术决策都记录在这里。以后让 Claude 改代码前，先让它读本文件，避免推翻已验证的结论。
>
> **协作规则（用户 2026-09-06 定，全开发期有效）**：
> 1. 任何技术问题，用户不提供具体要求；由 Claude 列出 2~3 个候选方案并逐一分析优劣，用户拍板后执行。
> 2. 每次拍板记入本文件。
> 3. 附加功能必须先向用户提出并说明理由，经用户确认后方可添加。

---

## 决策 1：产品形态 — 桌面 APP（2026-09-06）

- **背景**：用户拒绝上云（不花服务器/域名费用、不等 ICP 备案）。
- **结论**：开发桌面 APP「实验室助手」，运行于 Windows + Mac，数据全本机存储，问答直连阿里云百炼 API（按量约 1~2 分/问）。
- **备选**：GitHub 托管网页版（需服务器）；微信小程序（个人主体医疗健康类目过审难 + 强制备案域名）。
- **回退方向**：APP 稳定后若获得企业资质，可把小程序的壳套到同一套后端上。

## 决策 2：技术栈 — Python + pywebview（2026-09-06，用户拍板）

- **备选**：Electron 全 JS（RAG 需重写、中文文档解析弱）；Electron + Python 双引擎（打包最复杂、体积 200~300MB）。
- **选定理由**：一门语言贯穿前后端；复用 Dify 项目全部 RAG 经验；打包路径最短；新手维护最省心。
- **回退方向**：无（框架级决策，不再回退）。

## 决策 3：附加功能范围（2026-09-06，用户拍板）

- **本期做**：笔记导出（Word/Markdown，含引用原文）、多知识库分类（内置 QC 库 + 自建课程库）。
- **二期**：图片 OCR 入库（中文手写/公式识别效果有限、技术最重）。
- **核心自带**：引用原文点击查看、多轮追问、拒答防幻觉、会话历史。

## 决策 4：API Key 策略 — 内置 + 可替换（2026-09-06，用户拍板）

- **结论**：APP 自带一个内置 Key 开箱即用；设置页可换成用户自己的 Key。
- **风险与兜底**：内置 Key 理论上可被从安装包提取盗用 → 百炼控制台设「单 Key 限额 + 用量告警」，内置 Key 只充小额。
- **备选**：仅内置（无替换出口）；用户自填（门槛高、演示体验差）。

## 决策 5：向量存储 — sqlite-vec（2026-09-06，随计划审批）

- **理由**：一个 SQLite 文件装下全部数据（块+向量+文件哈希表），无重型依赖；chromadb 依赖树沉重且 PyInstaller 打包是公认痛点；语料仅几百~几千条。
- **备选/回退**：numpy 暴力检索（接口已抽象，打包遇阻时一天内可换）。
- **已解决的技术坑**：KNN 查询在 JOIN 场景必须用 `k = ?` 显式约束（不能只靠外层 LIMIT）；查询向量必须归一化后 MATCH（否则分数全 0）。

## 决策 6：向量化 — 百炼 text-embedding-v4 API（2026-09-06，随计划审批）

- **理由**：零模型下载（本地 fastembed 需从 HuggingFace 下模型，国内校园网不稳）；embedding 与 LLM 同一供应商、一把 Key；语料小成本可忽略。
- **隐私说明**：问答时检索到的文档片段本就会发给 LLM，本地向量化只解决一半隐私问题；PRD 中明示知情。
- **回退方向**：`embeddings.py` 只暴露 `embed()` 一个接口，将来换本地模型只改一个文件。

## 决策 7：后端 HTTP 层 — Flask（2026-09-06，随计划审批）

- **理由**：纯 Python 小依赖、打包无痛；单机 localhost 用 werkzeug 自带服务器足够；SSE 流式用生成器即可实现。
- **已解决的技术坑**：必须 `Flask(__name__, static_folder=None)` 禁用内置 /static 路由，否则自定义 /static 路由被抢先匹配导致静态资源 404。

## 决策 8：分块策略 — 按行/自然段，不按固定长度（2026-09-06，沿用 Dify 结论）

- **txt**：每行一条完整知识点（语料按此格式编写，对任何切分都健壮）。
- **pdf/docx**：按自然段落，短段合并至 800 字、超长段按句号分号断句。
- **扫描版 PDF**：文字层缺失占比 >70% 时报错提示 OCR 二期。

## 决策 9：防幻觉策略（2026-09-06，沿用 Dify 验证过的铁律）

- 提示词铁律（prompts/qa_system.txt）：只依据参考资料 / 每个结论紧跟 [n] / 查不到说「资料中未找到相关内容」/ 只能删减不能增补 / 温度 0。
- **无命中直接拒答**：检索零命中时不调 LLM，直接返回拒答语（省钱 + 防幻觉）。
- 检索参数：top_k=6、阈值 0.3，设置页可调；召回调试视图可排查「资料里有却答不出」。

## 决策 10：打包 — PyInstaller onedir + zip 分发，不做安装器（2026-09-06，随计划审批）

- **理由**：onefile 启动慢且杀毒误报率显著更高；未签名安装器同样触发 SmartScreen，多一层工具链无收益。
- **用户数据**：全部放 `%APPDATA%\LabAssistant\`（索引可再生，永不作为唯一数据源）。
- **WebView2 缺失**：启动捕获异常弹中文提示框 + 官方 Evergreen 下载链接。
- **已知风险**：PyInstaller 产物可能被杀软误报 → 不用 UPX、依赖精简；极端情况降级为 venv 源码 + bat 启动。

## 决策 11：前端 — 纯静态 HTML/CSS/JS，无构建工具、无 CDN（2026-09-06，随计划审批）

- **理由**：不装 node、不编译，新手可维护；校园网不可靠故不引 CDN，Markdown 渲染用自写迷你渲染器（md.js）。
- **文件上传**：用 `<input type=file multiple>` + `webkitdirectory` 走 HTTP 上传（不依赖 pywebview 原生对话框，浏览器里同样能测）。

## 决策 12：增量索引 — 文件 md5 哈希跳过未变化文件（2026-09-06，随计划审批）

- **实现**：重建时未变化的文件从旧索引整文件复制块（免重新向量化省钱省时）；新增/修改文件才解析+向量化；删除的文件自动消失；最后原子替换索引文件。

## 决策 13：多轮追问改写（2026-09-06，验收发现翻车后补上）

- **问题**：验收第 1 组多轮中「它分几种方法？」检索到紫外分光光度法的内容，模型答错对象。
- **结论**：检索前先让 qwen-plus 把追问改写为独立完整问题（补全「它」「该法」指代），**改写只用于检索**；回答仍用原问题+对话历史。改写失败降级用原问题，不阻塞。
- **理由**：这是经典的多轮 RAG 问题（追问查询太短导致检索跑偏）；Dify 版靠 qwen3-rerank 重排侥幸通过，自研版用改写更省成本、更可控。
- **验证**：改写后 3 组多轮全部通过。

## 决策 14：打包实测结果（2026-09-06）

- PyInstaller onedir 打包成功：143MB（zip 65MB）；exe 名 LabAssistant（ASCII 稳妥，窗口标题为中文「实验室助手」）。
- 打包要点：spec 里 `APP_ROOT = os.path.dirname(os.path.abspath(SPECPATH))`（**SPECPATH 是 spec 所在目录而非文件路径**，曾因此报错两次）；sqlite_vec/webview/pythonnet/clr_loader/pymupdf/charset_normalizer 用 collect_all 整包收集；webview 自带 `__pyinstaller/hook-webview.py` 自动生效。
- 打包版实测通过：窗口打开、静态资源全 200、端到端问答（必杀题）流式输出 + 引用正常。
- 一键重打包：build\build_win.bat；分发物：dist\实验室助手-v0.1.0-win64.zip（含使用说明.txt）。

## 决策 15：git 版本控制 + 密钥移出代码（2026-09-06，用户拍板）

- **仓库范围**：只管理 d:\wibecoding\app（用户拍板）；dify/、旧 venv 等不纳入。
- **云端备份**：Gitee 私有仓库（用户拍板；GitHub 因校园网不稳定被否，仅本地因不防硬盘损坏被否）。
- **密钥移出代码**（用户拍板）：Key 存 app\local_key.txt（.gitignore 排除）；config.py 用 `_load_local_key()` 启动时读取；打包 spec 的 datas 把该文件带进包，开箱即用不受影响（已实测 has_key=true）。**以后要发不带 Key 的版本，删除 local_key.txt 再打包即可。**
- **用户日常操作**：双击 保存版本.bat（本机快照）/ 同步云端.bat（推 Gitee）；单分支 master；回滚由 Claude 讲解后代操作。
- **踩坑**：PyInstaller 的 workpath 是命令行参数（--workpath），Analysis() 不接受该参数；spec 中 APP_ROOT 用 os.path.dirname(os.path.abspath(SPECPATH))（SPECPATH 是 spec 所在目录）。
- **安全注意**：Gitee 令牌明文存本机 .git-credentials（credential.helper=store），仅本机；local_key.txt 在 app 目录，勿随文件夹整体拷贝外发。
- **已上线**（2026-09-06）：远程 https://gitee.com/yuyuyuuyuyuy/lab-assistant（私有），3 张快照已同步（本地 HEAD=云端 746a31d）。踩坑：首个令牌被 Gitee 拒（"Incorrect username or password"），用户重新生成令牌后成功——**Gitee 令牌可能生成时权限未保存完整，失败时先重新生成令牌**；git push 无 TTY 时会无限挂起，务必 `GIT_TERMINAL_PROMPT=0 timeout 60` 包裹。

## 决策 16：GitHub 开源（2026-09-06，用户拍板）

- **用户拍板**：公开仓库**不含语料**（药典版权风险）+ **MIT 许可证**（署名 wibecoding，可改）。
- **架构**：独立精简仓库 d:\wibecoding\app_public\（干净历史、48 文件、语料仅留 README 占位、无 local_key），推送 https://github.com/yuyuyuuyuyuy/lab-assistant（public，main 分支）；主仓库 app\ 继续推 Gitee 私有备份，两者互不干扰。
- **同步维护**：代码变更由 Claude 负责同步进 app_public（复制→提交→推 GitHub）；用户日常双击习惯不变。
- **凭据**：GitHub 令牌与 Gitee 令牌同存本机 .git-credentials（credential.helper=store）。
- **新增文件**：README.md（中文完整说明）、LICENSE（MIT）、requirements.txt（锁版本）、res/builtin_kb/docs/README.md（语料目录占位）。
- **安全自检**：推送前已 git grep 确认代码中无 API Key/令牌；公开内容不含语料与密钥。

## 决策 17：OCR 引擎 — 百炼云端 qwen-vl 多模态（2026-09-08，用户拍板）

- **背景**：二期「拍照识字入库」。候选三方案：A 百炼云端 qwen-vl；B 本地 RapidOCR；C 云端+本地混合。
- **结论**：**方案 A**。复用现有百炼 Key 与 OpenAI 兼容 client，零安装、打包零负担；中文手写+公式识别能力本地引擎望尘莫及（用户核心场景就是手写笔记）。费用每张约 1~2 分。
- **否决理由**：B 手写识别差、公式不支持、打包增重 100MB+；C 双引擎维护复杂度翻倍，且问答本就需联网，离线 OCR 收益有限。
- **实现**：backend/ocr.py（Pillow EXIF 校正+长边 3000px 等比压缩→base64 data URI）+ `recognize_image` 走 chat.completions 多模态 content 数组；默认 `ocr_model=qwen3-vl-plus`（设置页可换 qwen-vl-max-latest 手写最强 / qwen-vl-ocr-latest 印刷体最省）；**已实测兼容模式 base64 图片可用**。
- **隐私**：OCR 页明示「图片将发送至百炼识别」。

## 决策 18：多知识库任选（kb_ids 数组化）（2026-09-08，用户拍板功能，方案随计划审批）

- **结论**：conversations.kb_id 列内容改为存 JSON 数组字符串（旧值 "all"/单 id 零迁移兼容，`store.parse_scope` 统一解析）；/api/chat、/api/search、POST /api/conversations 接受 `kb_ids` 数组（兼容旧 kb_id 单值）；`_gather_hits` 统一走 `search.search_multi`（单库也走多库路径，命中自动带 kb_name，引用展示更完整）。
- **前端**：App.state.kbScope 单值 → kbIds 数组；聊天头部下拉改为**多选弹层**（复选框：全部/各库+文档数+已选计数）；侧边栏点库名=快速单选；切换即开新对话（沿用旧交互直觉）。
- **验收**：tests/test_multikb.py 13 项全过（多选命中带库名、旧数据兼容、delete_last_assistant 等）。

## 决策 19：扫描版 PDF 整本 OCR — sidecar 方案（2026-09-08，用户拍板功能）

- **结论**：OCR 结果写入与原 PDF 同目录的 **「文件名.pdf.ocr.json」**（sidecar，`{"pages":[{page,text}]}`）；增量索引收录 sidecar：每页文本分段、chunk meta 带 `page`、source=原 PDF 名；**有 sidecar 的 pdf 跳过 parse_pdf**（免报错噪音）；files 表记 sidecar 的 md5（sidecar 变化才重新向量化，复用增量索引）。
- **否决方案**：把 OCR 文本拼成一个大 txt 入库——页码混入正文、切块跨页、污染检索文本。
- **实现**：kb.start_pdf_ocr 后台线程（pymupdf 逐页渲染 dpi150 → 并发 2 调 recognize_image、每页 3 次重试 → 写 sidecar → start_ingest）；OCR_STATUS 与 INGEST_STATUS 并列；引用展示「文件·第N页」（chat.py tag + extract_citations page 字段，file 去掉 .ocr.json 后缀）。
- **成本**：每页约 0.5~2 分；200 页约 0.2~1 元、10~20 分钟。中断需重跑（断点续传记三期）。
- **踩坑**：`os.path.splitext("讲义.pdf.ocr.json")[1]` 返回 ".json" 而非 ".ocr.json"——scan_docs 的 extra_exts 必须按**文件名结尾**匹配而非扩展名。

## 决策 20：二期 UI/交互决策包（2026-09-08，用户拍板全做）

- **引用折叠展开**：徽章点击 → 定位本消息引用列表第 n 条并展开+高亮动画（1.5s）；引用列表加「全部展开/全部收起」；**移除右侧抽屉 #source-panel**（与内联列表重复，遮挡聊天区；滚动定位体验更好）。
- **深色模式**：硬编码色抽成 CSS 变量 + `html.dark` 覆盖；设置页三选（跟随系统/浅色/深色）；持久化=settings.json `theme` 字段（单一事实源），localStorage 镜像 + head 内联脚本防启动闪白；auto 监听 matchMedia。
- **回答快捷操作**：复制（clipboard+execCommand 降级）/ 停止（AbortController 中断 fetch 流 + 新端点 POST /api/conversations/<id>/messages 补存半截回答保历史配对）/ 重新生成（/api/chat 加 `regenerate:true` → delete_last_assistant + 不重复存 user 消息）。streamAnswer 固定 convId 快照防中途切库写错会话。
- **会话历史**：「🕘 历史」弹层（标题/时间/知识库范围；点击载入 messages 渲染并同步 kbIds；删除走现有 DELETE）。
- **空状态引导**：KB 列表仅内置库时提示自建；所选库 0 文档时聊天空态提示上传；空态常驻「上传资料」「拍照识字」按钮。
- **杂项**：/api/settings 返回真实 data_dir；设置页新增 OCR 模型、外观主题字段。
- **验收**：E2E 实测——SSE 引用正常、regenerate 历史不重复（user×1 assistant×1）、多轮改写正常；新端点冒烟全过。

## 决策 21：增量索引两个并发/ID 缺陷修复（2026-09-08，真实手写照片验收揪出）

- **触发场景**：真实手写照片验收时，连续快速保存两张识别结果（ocr-save 各自触发 start_ingest）→ 索引报 `UNIQUE constraint failed: chunks.id`，第一份笔记索引丢失。
- **根因一（并发）**：两个 build_index 同时写同一个 `index.sqlite.tmp`，互踩。修复：kb.py 加每库 `threading.Lock`（INGEST_LOCKS），锁占用期间的调用记 INGEST_PENDING，本轮结束后自动补跑一次（增量幂等，最后跑一次覆盖全部变更）。
- **根因二（id 冲突，v0.1.0 遗留）**：copy_file 保留旧索引的块 id；当一轮重建中"新文件先分配 id 1..N、旧文件随后被复制（旧 id 也是 1..N）"时必然冲突。v0.1.0 没暴露是因为增量场景只有"全复制"没有"新旧混合"。修复：copy_file 在新库里**重新分配连续 id**（vec 行同步重映射）。
- **回归防护**：tests/test_multikb.py 新增第 9 组检查（2 文件建索引 → 加 1 新文件混合重建 → 全量复制重建，共 3 项）。
- **修复后验收**：两照片入库 73 块零报错；检索"道尔顿分压定律/体积功定义式/波义尔定律"全命中；提问"体积功定义式"回答带 3 条引用（含原文与相似度）。

## 决策 22：三期需求拍板（2026-09-09，用户拍板）

三期 4 条需求，逐项拍板如下：

- **①按键分区+高亮**：侧边栏分三组——「知识库」（列表+新建）、「学习工具」（📷拍照识字、📝知识笔记、📄我的笔记，主题色高亮按钮）、「系统」（🕘历史移入、⚙设置）。否决：输入框上方工具栏（占聊天区）、两者结合（重复入口易乱）。
- **②首次引导**：分步向导弹窗（欢迎+Key → 知识库 → 上传资料 → 拍照识字/PPT → 知识笔记 → 完成），可跳过，完成/跳过后不再出现，设置页可重新查看。否决：气泡式提示（分散）、静态图文页（单薄）。
- **③PPT 入库**：混合方案——python-pptx 提取文本层（零 API 费用、秒级）；某页文本过少（<10 字，图片型页）时自动提取该页内嵌图片送 qwen-vl OCR 兜底（约 1~2 分/页）。否决：纯文本提取（图片页无内容）、全页渲染 OCR（需 LibreOffice 外部依赖、慢、贵）。**仅支持 .pptx（老 .ppt 需另存）**。
- **④关键词知识笔记**：完整版——检索（top_k=16）→ LLM 按笔记模板整理（定义/要点/公式/易错，每结论带 [n] 引用）→ 可保存到「我的笔记」（chat.db notes 表，列表查看/删除）、可导出 MD/Word、可一键加入自建知识库（写 txt 入库参与问答）。否决：全库扫描长文（贵慢）、只列原文（不满足需求）。
- **开发顺序**（我定）：P1 PPT 入库 → P2 知识笔记 → P3 侧边栏分区 → P4 首次引导 → P5 收尾打包 v0.3.0。
- **实现取舍**：PPT 图片页 OCR 在索引构建时同步执行（图片页通常少、进度条可见）；OCR 失败降级为该页空文本、不阻塞入库（warning 计数）。笔记生成走非流式（等待可接受，先简后优）。

## 决策 23：三期实施记录（2026-09-09，P1~P4 全部完成验收）

- **P1 PPT 入库（混合方案）**：`parser.parse_pptx` 递归提取文本层（组/表格/图表数据/备注），图片型页（文本<10 字）提取页内图片（含组内嵌套）并发 OCR 兜底（每图 2 次重试，失败降级该页空文本、不阻塞入库）；每页一块 chunk（meta 带 page，超长页分段，短页不过 split_paragraphs 的 8 字过滤）；SUPPORTED_EXTS 加 .pptx，**上传白名单三处硬编码统一改为引用 parser.SUPPORTED_EXTS**（kb.import_files/import_folder/server.upload_docs）。验收 8 项全过（tests/test_ppt.py）。打包 spec collect_all 加 "pptx"。
- **P2 知识笔记（完整版）**：chat.db 加 notes 表；POST /api/notes/generate（top_k=16 → note_system.txt 模板整理 → 带引用）；保存/列表/删除；导出复用 export 模块（export_note_text，md/docx，docx 按 # 层级转标题）；一键入库 note-save 与 ocr-save 共用 `kb.save_text_doc`（文件名清洗+同名加序号，永不覆盖）。验收 14 项全过（tests/test_notes.py）。
- **踩坑（重要，防幻觉双防线的新认知）**：笔记检索本想放宽阈值到 0.25，实测无关关键词「量子色动力学重整化群方程」仍命中 16 条（分数 0.35~0.39，全部来自手写笔记库的物理化学内容）——**embedding 无法区分同领域不同主题（物化 vs 高能物理）**。结论：**阈值保持 0.3（验证过的拒答线），同领域主题漂移由模型铁律拒答兜底**（实测正确输出「资料中未找到相关内容」）。双防线分工明确：阈值挡不同领域（省钱不调 LLM）、模型铁律挡同领域漂移（花 1 分钱但结果正确）。测试固化双防线两个用例。
- **P3 侧边栏分区**：三组——知识库（列表+虚线「＋ 新建知识库」按钮）/ 学习工具（📷拍照识字、📝知识笔记，主题色渐变高亮）/ 系统（🕘历史对话移入、⚙设置）。纯 HTML/CSS 改动，JS 零改动（所有元素 id 保留）。
- **P4 首次引导**：5 步分步向导（欢迎+Key → 知识库 → 资料入库 → 拍照识字 → 知识笔记），可跳过；onboarding_done 置位后不再出现；设置页「重新查看使用引导」；第 1 步填的 Key 在完成时一并保存；老用户升级后也会看到一次新版引导（settings 无该字段时默认 False）。

## 决策 25：产品包装（0→1 产品化）+ UI 设计风格（2026-09-15，用户拍板）

- **背景**：用户要求以资深 PM 视角把 APP 包装为「从 0 到 1 已落地项目产品」。差距分析结论：技术资产强（16 功能、金标评测 25/25、防幻觉双防线、文档齐全），缺的是证据层（真实用户数据/埋点/反馈）、展示层（图标 VI/落地页/Demo）、信任层（签名/Releases）。
- **拍板（用户）**：包装场景=**多场景综合**（主材料+按场景裁剪）；P0 启动=真实用户测试、Releases+落地页、一页纸介绍+APP 反馈入口、**UI 优化**；Mac 版构建与 Demo 视频暂缓。
- **UI 设计风格=D 现代学术风**：现代极简布局骨架 + 药典蓝绿品牌色（主 #1a4d80 药典蓝 / #0f5c4a 墨绿、纸张米白底 #faf9f6、印章红点缀）+ 引用块保留「文献摘录」质感（米黄底+竖线）+ 标题用衬线字体（宋体系）+ 深色模式同步重调。否决：A 纯学术风（显老气）、B 纯极简（无个性）、C 科技渐变（学习场景易疲劳、实现成本高）。
- **应用图标（logo）随风格一起做**：圆形徽章+「鉴」字设计，产出 ico（exe/窗口）+ 落地页 favicon。SVG/PIL 生成。
- **执行顺序（我定）**：UI 风格改造 → 图标 → APP 反馈入口 → 一页纸介绍 → 用户测试计划（用户找 5~8 位同学执行）→ 重新打包 → GitHub Releases 正式发布 + GitHub Pages 落地页。

## 决策 24：APP 更名「药鉴」（2026-09-10，用户拍板）

- **背景**：用户为 APP 定名「药鉴」（寓意药物鉴定/检验）。
- **全平台同步范围**：界面（窗口标题/侧边栏品牌/空状态欢迎语/引导向导）、AI 提示词（qa_system/note_system 与代码兜底）、导出文件页脚、文档（产品文档/需求调研/使用说明/公开 README）、脚本提示、打包产物（exe 名 YaoJian、zip 名「药鉴-v0.3.0-win64.zip」）。
- **保留不动的三处**（防丢数据/断链接）：①数据目录仍为 `%APPDATA%\LabAssistant\`（改名会让已有用户的知识库与对话"消失"，收益为零）；②Gitee/GitHub 仓库名仍为 lab-assistant（改仓库名需网页操作且旧链接失效，想改时另行指导）；③本文件历史决策条目中的旧名「实验室助手」如实保留（历史档案不改写）。
- 本决策之前的历史条目中「实验室助手」即「药鉴」曾用名。

## 决策 24：金标评测系统（A1）+ 语料统计面板（A3b）（2026-09-10，用户拍板 1A/2A/3A）

- **背景**：对应健康产品岗 JD 方法论（金标验证 / 数据质量监控），把「验收靠人工一次性」升级为「可量化 + 可回归」体系。
- **A1 自动评测**（tests/eval.py + tests/eval_dataset.json）：
  - 评分三道关：①溯源检查（规则：回答引用文件名必须命中金标指定子串——防"答对了但依据是错的"）；②要点覆盖率（LLM 裁判 qwen-plus 温度 0，逐条判断金标要点是否覆盖，JSON 输出）；③拒答检查（必须输出"资料中未找到相关内容"）。
  - 及格线：溯源必过 + 要点覆盖 ≥ 60%；多轮追问逐轮溯源，全过才算该组通过。
  - 每次运行产出 tests/eval_reports/eval_时间戳.md + latest_summary.json，与上次对比输出退化/修复清单（回归）。
  - 费用约 0.8~1 元/轮；--dry 免费校验数据与索引。金标数据（20 单题 + 3 组追问 + 2 拒答）由 Claude 依内置语料起草，**待用户审核**。
- **A3b 语料统计面板**：
  - 索引统计从内存 INGEST_STATUS 升级为持久化台账 `kbs/<id>/ingest_stats.json`（成功/失败都落盘，临时文件原子替换）。
  - 新 API `GET /api/kbs/<id>/stats`：台账 + files 表每文件块数 + 未入库文件清单（有 sidecar 的扫描版 PDF 视为已入库）。
  - 前端：设置页第三标签「语料统计」——摘要卡片（文档/块数/新增/复用）+ 上次索引失败提示 + 未入库/错误清单 + 文件明细表（按块数倒序）；tab 切换逻辑泛化为 data-tab 通用。
  - 验收：tests/test_stats.py 冒烟全过（内置库增量重建 10 文件全部复用、零 API 费用；台账落盘；不存在库 404）。
- **待办**：打包 v0.3.1 时把 tests/ 排除在发行包外（现状已排除）。
- **实战记录（2026-09-10）**：首轮基线 24/25（唯一挂题 03=金标要点定严了，把问题没问的电导率列为必答）；用户拍板修法——03 改问题（加"电导率要求"）、02/06 删偏题要点，定下原则「**金标要点 = 问题明确要求的内容**」；二轮 25/25 全过，回归对比首次实战（修复 single-03、退化 0）。

## 决策 26：0→1 流程 S1 执行与用户研究文档重构（2026-09-16，用户拍板）

- **背景**：启动企业级 0→1 流程逐步执行（每步五段循环：方案→执行→评审→优化→记录）。S1=用户研究与痛点假设，产出深化版第二章后用户提出 5 条评审意见，逐条评价后拍板如下。
- **拍板 ①文档去现状**：选「归档重写」——旧文档改名 `需求调研文档_v0.1_历史归档.md`，新建 v1.0 从零视角文档（假定产品未开发，删所有「已实现/决策 N」表述与 1.5 现状对照表；第一章业界调研内容保留，「对本项目的启示」改写为中性「调研启示」；第三章需求清单改候选态）。
- **拍板 ②用户群调整**：重点=学生群 A（细分 3 类：课程学习/期末备考/QC 实习衔接）+ **新增用户群 C 考研药学考生**（重点群，痛点 P12~P15，付费假设 H8 扩展至 C 群）；**用户群 B 药企 QC 降级为次级观察群**（形态冲突：SOP 涉密 vs 直连公有云 API；无付费动力+采购链长；渠道缺失；验证资源优先 A/C 群）。删除 A4 细分（stakeholder≠user，发起人求职动机不进入用户细分）。
- **拍板 ③卖点方向**：防幻觉/引用可信=信任背书（保健因素），不是主卖点；卖点从学生 JTBD 提炼（"快速找到可靠答案""把零散资料变成能背的笔记"），S3 机会评估时正式重定义。
- **保留未拍板（待后续步骤）**：5 处验证缺口处理方案（P4→H9、P6→H10 映射错位修正、C 群 P12~P14→H11~H13 假设设计、P9/P10 暂缓）——S11 用户测试执行前必须拍板。
- **评审纪律记录**：用户 5 条意见中，4 条被采纳（去现状、卖点、B 群、A4），1 条部分采纳（1.2.5 拒答机制——内容本身保留为调研结论，现状对照部分随拍板①移除）；Claude 对每条意见给出了独立评价（含事实核实：chat.py:110 固定拒答语属实）。

## 决策 27：S1 二轮评审拍板（2026-09-16，用户拍板）

- **B 群内容精简后置**：B 群画像压成一段话、场景合并；痛点从 5 条精简为 3 条（P11~P13：查条款慢/合规核对/数据合规，「新人答疑」删除）；痛点表与需求清单中 B 群全部排在 C 群之后。
- **P15 付费意愿移出痛点表**：付费是商业化假设而非用户痛点（痛点回答"产品解决什么问题"，付费回答"用户愿不愿意买单"），归入 H8。
- **H5 口语化转 Claude 离线验证**：不占用户测试任务，由 Claude 生成口语化改写样本跑离线检索实验（可随时执行）。
- **原 H6（QC 语料覆盖）/H7（QC 合规核对）删除**：B 群降级后无验证资源，暂缓不再保留假设编号。
- **验证缺口 5 处全部拍板**：①P4→新增 H6（PPT 导入意愿）②P6→独立 H7（笔记有用度）③P13 数据合规→暂缓 ④新人答疑→删除 ⑤C 群 P8~P10→新设计 H9（资料整合）/H10（背诵材料）/H11（真题考频需求探针）。
- **假设清单终版**：H1~H7 用户群 A（H5 离线）、H8 商业化、H9~H11 用户群 C；编号已重排，用户测试计划.md 的 H 编号对应关系将在 S11 前同步更新。

## 决策 28：S2 评审——四维矩阵第四维补强（2026-09-16，用户拍板）

- **背景**：用户评审 S2 竞品分析时指出「本地私有」维度对学生吸引力不足（光明日报调查数据焦虑仅 3.73/5；学生行为上普遍用云端免费工具；该维度对已降级的 B 群才是硬需求）——批评成立，四维矩阵第四维站不住。
- **拍板**：第四维由「本地私有」替换为**「学习闭环」= 背得会 + 考得上**（「本地私有」降级为加分项）。
  - **背得会（一期）**：知识笔记一键生成问答卡 + 间隔重复复习（艾宾浩斯）+ 卡片回溯引用原文（依据：Anki 刚需但制卡成本高；C 群痛点 P9）。
  - **考得上（二期）**：用户上传真题学题型/难度（粗粒度、**不承诺押题**）+ 基于知识库自测出题 + 追问历史个性化出题；「学得深」（追问式学习）并入「考得上」（用户方案，聚焦正确——离付费更近）。
- **Claude 两项形态修正（用户未反对）**：①「AI 主动追问」→「用户主动进入自测模式」（避免骚扰，AI 主动弹题在桌面学习工具中体验差）；②出题必须带引用 + 第一版只做填空/单选/名词解释（答案唯一可验证）——出烂题伤害「可信」口碑，「考得上」须建金标出题评测验收。
- **打包逻辑**：背得会引流（人人需要）、考得上增值付费（考研刚需）。
- **落地**：市场与竞品分析.md 2.3 矩阵与机会空白已重写；需求调研文档 C2/C3 需求、H10/H11 假设、P9/P10 证伪条件已同步更新。

## 决策 29：S2 渠道观察回填与新竞品发现（2026-09-16）

- **素材**：用户收集 15 张小红书截图（药学考研经验贴/笔记分享/AI 工具帖/Anki 体验帖/付费商品帖）放入 用户测试反馈/ 文件夹；截图 Read 工具不支持直接读取，经 PIL 转 PNG + qwen-vl OCR 转录（成本约 0.3 元）后分析回填。
- **回填**：市场与竞品分析.md 第三章——素材分析表 K01~K13（用户原话/对应痛点/支持反驳）+ 结论（P9 证据最扎实 5 条、H8 付费证据链成立、4 条设计修正输入）；需求调研文档 2.5 表 P2/P5/P8/P9/P10 证据列加「渠道观察」。
- **新竞品发现（补入 2.1/2.2）**：①**NotebookLM**（记忆卡/Quiz/思维导图/音频讲解，学生自发安利）——「学习闭环」方向的最强验证信号也是最需注意的间接竞品，弱点是云端+无引用核对+国内访问不稳；②**AI背书匠**（切割/挖空/监督背诵/智能出题，¥6/3天已售59）——「背得会+考得上」组合已有付费产品，市场性直接佐证。两者均为云端+通用语料+无核对，**四维空白象限结论不变**。
- **关键设计输入**：①K02 思维导图是学生认可度最高的笔记形态（纳入 C2 形态选项）；②K04 刷题以选择题为主（验证「考得上」客观题优先）；③K08 学生对国内 AI 质量不满、愿为效果付费（质量口碑>低价）；④K12 卡片与资料一致性是刚需（卡片回溯引用原文=精准解法）；⑤K07 音频讲解列入观察（非本期）。

## 决策 30：NotebookLM 竞争策略——学习闭环照原计划全做（2026-09-16，用户拍板）

- **背景**：用户评审指出「面对 NotebookLM 的产品竞争力不够」。Claude 诚实评估：NotebookLM（记忆卡/Quiz/思维导图/音频讲解/免费）在产品完成度上碾压尚处文档阶段的药鉴；但①其在中国大陆访问不稳定、数据上传谷歌云端，多数目标用户用不上；②真正风险是国内大厂复刻功能；③大厂与谷歌都不愿做深的是「逐句引用核对+本地数据+垂直语料」三件事。
- **候选方案**：A 闭环轻量化+可信主战场（Claude 推荐）；B 照原计划全做；C 彻底放弃闭环。
- **用户拍板：B 照原计划全做**（背得会一期、考得上二期），理由：NotebookLM 国内访问不稳、多数目标用户用不上——竞争逻辑与「ChatGPT 强但国内学生用豆包」同构，抢占「国内可用的可信替代」空缺。
- **定位口径**：「中国学生的可信版 NotebookLM——中文药学、逐句可核对、资料不出国」。
- **风险应对保留**：①出题质量金标评测+不承诺押题；②音频讲解列入用户测试后评估项（百炼 TTS 成本低）；③差异化始终压在「逐句引用核对+本地数据+垂直语料」上，警惕国内大厂复刻。

## 决策 31：S3 评审拍板（2026-09-16，用户拍板）

- **卖点**：选 A 方向（可信+闭环+垂直三要素），主标语需简练化（候选见对话，拍板后回填 机会评估与立项.md 1.3）。
- **出题目标群扩展**：考得上覆盖 A 群期末+C 群考研（期末刷题与考研刷题同一需求不同强度）；S3 用户侧表、价值地图、需求调研 C3 行已同步。
- **新需求立项：可定制知识笔记**（用户提案，Claude 评价赞同——与 K02 思维导图证据、K01 知识框架需求吻合）：关键词检索 → 分类展示模块（定义/原理/方法/公式/易错等）→ 用户勾选 → 按需生成；格式可选文本/思维导图（大纲渲染树形图，成本低）。需求调研 A6 行、S3 范围 3.1 已更新。实施属后续开发步骤。
- **音频讲解取消**（用户拍板：差异化纪律>跟随脚步）——不做 NotebookLM 招牌功能；S3 非目标表、竞品分析 2.3 已同步。
- **信任指标改造**（用户拍板）：总引用点击率是「验证性行为」指标——新用户点击是验收可信度，信任建立后点击率自然下降，不能当增长指标。改为：新用户前 3 次问答引用核对率（信任建立检查 ≥70%）+ 陷阱题定期抽测发现率（质量监控）+ 拒答后继续提问率；点击率降为健康度参考。S3 4.2 已更新。

## 决策 32：S4 需求分级与评审修正（2026-09-16，用户拍板）

- **产出**：docs/需求分级.md——15 条需求 KANO 分类 + RICE 排序，分级结论：P0=A5 防幻觉/A2 引用核对/A1 多库；P1=C1 资料整合/C2 背得会/A7 口语化/A6 可定制笔记/A4 PPT/A3 OCR；P2=G1 拒答原因/G3 评测/C3 考得上/G2 保真；暂缓=B1/B2。
- **评审修正 1（A7 改判基本型，用户拍板）**：原判兴奋型忽略了 KANO 时间漂移——所有通用 AI 已理解口语化，学生心智锚点「AI 听懂人话=理所应当」；多轮指代消解同理。RICE 同步升 P1（Impact 1→2，分数 0.39→0.77），挂「H5 离线验证闸门」（失败率高则升 P0）。
- **评审修正 2（G1 改判期望型，用户拍板）**：用户理由「没有付费点」——Claude 修正理由：KANO 类型由用户感知决定而非付费点；真正理由是拒答质量是连续光谱（无原因→原因→建议，Performance 曲线）+ RefusalBench 证据（统一话术引发困惑=存在不满）。
- **待办**：H5 离线实验（口语化改写检索失败率）可由 Claude 随时执行，校准 A7 的 RICE。

## 决策 33：H5 实验执行——口语化假设证伪、A7 拆分（2026-09-16）

- **实验**：tests/h5_colloquial.py（内置 QC 库：10 书面题+30 口语改写）+ h5_colloquial2.py（自建「手写笔记验收」物化库：5 书面+15 口语），Claude 手写口语化改写样本，检索 top_k=6 阈值 0.3。费用约 0.1 元。
- **结果（H5 不成立，P7 证伪）**：内置库书面 0% 未命中/均分 0.853 vs 口语 0%/0.820；自建库书面 0%/0.841 vs 口语 0%/0.799——两库均无显著差距。原因：口语化问法保留核心术语（"重金属/HPLC"），embedding 靠术语命中；内置库行级语料与自建库笔记文本均对问法鲁棒。
- **A7 拆分**：A7a 单轮口语化改写——证伪，降为观察项（S11 真实用户测试观察真实口语化行为，有失败案例再立项）；A7b 多轮指代改写——保留 P1（基本型；决策 13 真实验收证据：不改写则「它分几种」检索跑偏）。RICE：A7b=7×2×85%÷8=1.49。
- **对用户拍板的影响（如实记录）**：决策 32 用户拍板「A7 基本型」所依据的 H5 假设（口语化检索失败）被实验推翻——A7b 的基本型判断成立（依据改为指代消解），A7a 不成立。实验报告存档 tests/eval_reports/h5_colloquial.txt 与 h5_colloquial2_自建库.txt。
- **A7 拆分方案拍板（2026-09-17 用户）**：方案一——删 A7a 留 A7b。A7a 观察项兜底=S11 真实用户测试观察口语化行为（有失败案例再立项，开发 1 天可逆）；A7b 保留 P1。

## 决策 34：S6 评审拍板（2026-09-17，用户拍板）

- **自测指标修正**：删除「答题对/错」埋点——正确率是用户的学习指标不是产品健康指标（答错可能说明题有区分度）。改为：自测 7 天回访率 + 自测完成率（中途退出率）+ 自测结束页内嵌微型问卷（👍/👎，1~2 题无额外界面）+ 单题 👍/👎 反馈（兼作出题质量免费标注数据）。数据埋点方案.md 事件清单已改造（quiz_completed/quiz_rated/quiz_card_rated）。
- **收费模式方向（S17 正式展开）**：产品收入≠API 成本——钱全流向百炼则产品零收入。方向：卖软件价值（「考得上」为天然付费点：模拟卷市场 200~500 元、AI背书匠 ¥6/3 天已验证）；问答/背得会免费或成本价引流；个人项目无支付资质，充值入口用「激活码/卡密」模式或免费+赞赏。
- **新需求 A8 多模型服务商支持（用户提案）**：OpenAI 兼容协议下开放服务商 Base URL（DeepSeek/智谱/Kimi 等）；对话/OCR 模型自由换；**嵌入模型可换但需重建全部索引**（维度不同）。记入需求清单，S7 技术评审纳入。
- **PRD 补漏 F0 会话历史**：历史询问记录是基本功能（复习回看+S11 过程证据），补入 PRD P0（用户故事+验收）；自测历史与错题记录并入 F10 验收（错题重练=自测价值闭环）。V0.1 路线图同步更新。

## 决策 35：PRD 深度评审修订（2026-09-17，用户拍板）

Claude 批判性审视 PRD 提出 6 处问题，用户逐条拍板：
1. **F5 防积压机制**（采纳修法）：每日卡量上限+积压降载不滚雪球——K11 教训：Anki 第一痛点是积压恶性循环，不加防积压就是「Anki 第二」；
2. **F8 重定义为考研场景包**（用户选 B 方案）：非独立功能，=F1/F2/F3 的考研场景化封装；增量价值=真题/讲义模板化导入引导+考点标记；
3. **F13 数据备份与迁移**（采纳）：本地私有定位的完整性——数据归用户必须让用户带得走（zip 全量导出/导入恢复，不含敏感凭据）；
4. **新增第四章交付物与运行要求**（采纳）：合同写明交付形态（解压即用 zip/WebView2 兜底/引导/已知限制披露/数据目录）；
5. **F6 模块分类动态生成**（用户拍板）：先检索内容由模型判断实际具备哪些模块，只展示有内容的，不硬凑空模块；
6. **F1 增量索引三行为**（采纳）：未变跳过/变更重建/删除移除——索引与资料始终一致，防「搜到旧答案」砸可信招牌。

## 决策 36：PRD 二轮深度评审修订（2026-09-17，用户拍板全采纳）

1. **F4 弱相关硬凑场景**：双防线中间地带——弱相关命中（分数接近阈值）时模型不得拼接无关内容作答，须缩小范围或明示「仅找到部分相关内容」；
2. **F4×F9 矛盾消解**：F9 拒答原因与建议由**规则模板生成、不调用模型**（零命中保持零成本）；智能建议另立需求走 P2 验证；
3. **F3 引用错误反馈按钮**：引用展开面板加「此引用有问题」——信任闭环+免费质检数据（兼作 F12 训练/评测素材）；
4. **F14 用量与费用可见**（决策 4 待办正规化）：本月估算用量+费用估算+额度耗尽前友好提示+余额查询入口；
5. **F2 上下文有界窗口**：最近 N 轮历史+必要时摘要（Kimi 30 轮失忆实证；防成本失控），窗口外信息缺失时提示重述；
6. **需求追踪矩阵**：F↔A/C/G↔H↔版本 映射表加入功能规格章末；
7. **小补丁**：F7 识别前明示图片发送云端；F13 恢复=覆盖式+确认提示。版本路线图 V0.2 加 F14。

## 决策 37：PRD 三轮深度评审修订（2026-09-17，用户拍板全采纳）

1. **断网降级规格**（交付物章节新增「网络依赖与降级」）：网络不可用时可浏览本地数据、网络功能明确提示+重试、不白屏不假死——校园网不稳定是 S1 画像明写环境；
2. **F15 意见反馈**：S11 用户测试计划依赖「设置→意见反馈」，PRD 必须承接——三通道（复制/本机/GitHub Issue 预填）+feedback_submitted 埋点（数据埋点方案同步）；
3. **模型变更管理（CCI 落成流程）**：F4 验收⑤——任何模型/提示词/阈值变更必须重跑金标评测（含拒答题）通过才可发布；
4. **F11 可操作性**：评测一键运行（脚本化，运行者=不写代码的发起人）+ 金标集维护规则（每新增功能模块配套新增金标题才算完成）；
5. **Mac 版观察项触发条件**：Windows 版 H1~H11 验证通过后再启动构建（先验价值再付平台成本）；
6. **运行硬件要求**：最低 4GB 内存/2 核（推荐 8GB）；检索响应 <1 秒；
7. **小补丁**：F0 会话标题自动取首问前 N 字可改名。V0.2 路线图加 F15。

## 决策 38：PRD 四轮深度评审修订（2026-09-17，用户拍板全采纳）

1. **F1 误删保护**：知识库删除二次确认+明示后果（N 文档 N 块不可恢复）+提示先导出备份（联动 F13）；
2. **F5 背卡前置引导**（K11 碎片化教训防另一半）：首次背卡提示先建整体认知；卡片跳回来源笔记入口；
3. **F10 出题成本闸门**：单次自测题量上限（如 20 题）+生成前预计费用+超限分批；
4. **交付物新增隐私说明**：随包分发一页纸《隐私说明》（数据全本机/仅片段发云端/Key 存储/无账号无追踪）——信任基础文件+营销资产；
5. **交付物新增更新机制**：启动检查 GitHub Releases 版本号，纯提示不自动下载（本地私有定位）；
6. **F6 公式导出保真**：Markdown 保留 LaTeX 源码、Word 用 Unicode 或图片；验收用含公式真实笔记；
7. **F8 考点标记 AI 自动+用户可修正**（标注可见可改，不强迫信任）。

## 决策 39：S7 技术架构评审自审修订（2026-09-17，用户拍板按建议修）

用户为技术小白无法审查技术文档，Claude 自审提出 6 处+2 小补丁，全部按建议修订：
1. **并发控制**（2.9 节）：后台建索引 vs 前台提问抢 SQLite 锁——每库 threading.Lock+待处理队列补跑增量，防「database is locked」；
2. **流式中断资源释放**（第一章数据流）：停止按钮=后端捕获断开信号立即中止 LLM 流，省 token+线程释放，半截回答补存保历史配对；
3. **更新检查设计**（第一章）：异步检查 GitHub Releases、超时 5 秒、失败静默（校园网不稳）、纯提示不安装；
4. **备份一致性**（2.9 节）：chat.db 导出用 SQLite backup API 在线备份，禁直接复制文件；
5. **日志与诊断**（第一章）：运行日志写数据目录（滚动限量）；F15 反馈可选附诊断日志（不含对话内容）；
6. **R1b 打包清单风险**：sqlite_vec/webview pythonnet/clr_loader/pymupdf/charset_normalizer 全部 collect_all+新机冒烟测试；
7. 小补丁：端口冲突自动换端口；Key 明文存储披露义务（隐私说明写明勿外发配置文件）。

## 决策 40：S9 质量体系自审修订（2026-09-17，用户拍板按建议修）

1. **陷阱题生效保障**：陷阱须预跑确认出入真实存在才纳入评测，不成立则调整或放弃（不能靠运气）；
2. **L1 命中口径**：命中=top-6 至少 1 条与金标主题相关；判定=来源文件关键词规则匹配；争议题人工判定记录；
3. **裁判公正性设防**：每轮随机抽 20% 人工复核（发起人药学背景可判）；一致率 ≥90% 降为 10%；不一致率高则调裁判提示词；
4. **专项测试运行方式**：双防线/增量索引自动化并入金标；公式/断网/备份/并发纳入 I4 新机冒烟清单；
5. **角色分工**：人工=发起人（药学内容判定）+ Claude（技术判定退化定位）；
6. **评测预算规则**：开发期合并变更再跑、单日 L2 费用上限 5 元、UI 类变更不触发 L2；
7. 小补丁：首次运行无基线→存为基线输出「基线建立」；金标基线=内置 QC 库（自建库由专项覆盖）。

## 决策 41：S10 内部验收方案自审修订（2026-09-17，用户拍板按建议修）

1. **走查表版本范围矛盾**（最重要）：F5~F15 属 V0.2/V0.3，V0.1 验收不可执行——走查表加「版本」列+执行规则「只走查当前已发布版本的行」；
2. **剧本版本适用性**：V0.1 验收=剧本 A 全流程+剧本 B 可用步骤（普通模式导入/提问/断网/反馈/备份）；V0.2 验收=完整剧本 B；
3. **双防线第二道补测**：剧本 A 步骤 5 补同领域漂移题（「药物的降压作用机制」验证铁律拒答）；
4. **断网恢复路径补测**：剧本 B 步骤 4 补「恢复网络后再问一题验证恢复正常+断网期间操作状态提示」；
5. **观察项闭环**：S11 中 3 名以上测试者独立遇到→升级 P1；无人遇到→关闭（保留记录）；判定模板写入 S11 回收；
6. **交付物走查行**：更新检查（临时改低版本号验证）/隐私说明逐条核对/使用说明按文档操作验证；
7. 小补丁：剧本记录表加「耗时」列（>预期=体验问题）。

## 决策 42：S11 用户测试计划 v2.0 修订（2026-09-17，随方案执行）

- **适配 H1~H11 终版**：H5 已离线完成不再入测试；新增 H6（PPT 导入）/H7（笔记有用度）测试任务与问卷题；H9/H10/H11 为 C 群专属任务；H8 分群对比（C vs A）；
- **执行时点定案**：V0.2 完成后（任务需要 OCR/可定制笔记/背得会/意见反馈/使用数据导出等 V0.2 功能）；
- **样本**：A 群 5 名 + C 群 5~8 名；B 群暂缓；测试包资料预置（不含测试者个人资料）；
- **数据回收三通道**：问卷 + 埋点导出 JSON（不含对话内容）+ 观察记录；观察项判定模板（3 人以上遇到→升级 P1）；
- **任务清单**：17 步（含 H2 双防线两道拒答题、A7b 多轮追问、F15 反馈验证）；保留旧版招募话术并新增考研版话术。

## 决策 43：北极星口径修正 + 埋点实现（2026-09-17，用户拍板）

- **北极星口径修正（用户拍板）**：用户指出「纯问答也是有效使用」——原口径以「点引用」为必要条件，与「引用点击是验证性行为、信任建立后自然下降」自相矛盾。修正：有效会话=会话内有**成功回答（命中且非拒答）**或笔记/卡片复习/自测完成；citation_clicked 移出北极星、归位信任过程指标。三处文档已同步（数据埋点方案/机会评估/PRD）。
- **埋点实现完成（S6 方案落地）**：
  - 新模块 backend/events.py（白名单 15 事件/500 字符截断/静默失败/last_answer/导出不含对话文本）；store events 表；
  - 接入：main.py app_launch、server 8 处（key_replaced 对比/api_key、kb_created、question_asked+refusal_followup 10 分钟判定+answer_done 带 hit 与引用数、note_generated、note_saved_to_kb×2、feedback_submitted、citation 端点）、kb.py doc_imported（索引完成时带成败数）；F5/F10 事件预留；
  - 新端点：POST /api/events/citation、GET /api/events/stats、GET /api/events/export（JSON 事件流+CSV 摘要）、DELETE /api/events；前端设置页新「使用数据」tab（events.js：统计卡片/导出双文件/删除确认）；chat.js 徽章点击上报；
  - 验收：tests/test_events.py 9/9 全过（隔离临时库）；E2E 实测（问答+拒答事件落库、有效会话口径正确、导出摘要）；**金标回归 25/25 退化 0**。前端面板待打包版实测。

## 决策 44：F14 用量与费用可见实现（2026-09-17，随方案执行）

- **补 ocr_recognized 事件**（S6 清单遗漏）：白名单+两个接入点——拍照识字按成功张数（server.ocr_images 末尾）、整本 PDF OCR 按页数（kb.start_pdf_ocr 完成处）。
- **GET /api/usage**：读本月埋点事件统计问答/笔记/图片识别/PDF 页数，按公开单价区间估算费用（问答 0.005~0.02、笔记 0.02~0.05、OCR 图 0.01~0.02、PDF 页 0.005~0.02 元），标注「以控制台账单为准」。
- **前端**：基本设置费用说明区升级为动态「用量与费用」区块（settings.js loadUsage，打开设置时刷新）；chat.js SSE error 余额关键词（arrear/balance/quota/余额/额度等）→ 友好提示（充值链接+自填 Key 引导）。
- **验收**：/api/usage 实测正确（2 问 → 0.01~0.04 元）；前端 UI 类变更按 S9 预算规则不触发金标；ocr_recognized 真实调用待打包版实测。

## 决策 45：F13 数据备份与迁移实现（2026-09-17，随方案执行）

- **新模块 backend/backup.py**：export_backup（chat.db 用 SQLite backup API 在线快照+kbs 全量打包，**不含 settings.json=Key 永不进备份包**）；stage_import（zip slip 路径穿越防护+chat.db 校验+摘要：知识库数/对话数）；apply_backup（覆盖式恢复：旧数据目录改名 `_old_时间戳` 保留逃生通道，恢复后沿用当前 settings——Key 与偏好不丢）。
- **端点**：GET /api/backup/export（zip 下载）、POST /api/backup/import（上传校验返回 token+摘要）、POST /api/backup/apply（确认后覆盖执行）。
- **前端**：基本设置「备份与恢复」区——导出下载/选文件/摘要确认（明示覆盖后果）/完成提示重启。
- **验收**：tests/test_backup.py 5/5（隔离环境：导出无 Key、摘要正确、覆盖语义、无效包、zip slip 防护、逃生通道、settings 沿用）；E2E 三端点全通。零费用。

## 决策 46：F6 可定制知识笔记升级实现（2026-09-17，随方案执行）

- **两步式**：①POST /api/notes/modules——检索 top16 后 LLM 动态分析（MODULE_POOL 8 候选模块，只返回有实质内容的，含每模块一句话描述）；②POST /api/notes/generate 扩展 modules+format 参数——提示词「只写选定模块，未选的一个都不写」；format=outline 时输出层级大纲（## → ### → 缩进要点）。
- **提示词**：note_system.txt 升级为模板（{modules}/{format_instruction} 占位符），铁律四条不变；chat.py 新增 analyze_modules + MODULE_ANALYZE_PROMPT（JSON 输出+解析容错）；generate_note 向后兼容（modules=None=全部候选）。
- **前端**：notes.js 两步交互（检索分析→模块勾选默认全选+格式单选（📄 文本/🧠 思维导图）→生成）；outline 渲染=renderMd+树形连接线 CSS（.outline-tree 竖线样式）；事件 note_generated 加 module_count/outline 字段。
- **验收**：E2E 实测——「重金属检查法」动态返回 6 模块（不硬凑记忆技巧等空模块）；勾选 3 模块+outline 生成只含选定模块、层级大纲正确、引用铁律保持；**tests/test_notes.py 14 项全过（向后兼容）**。费用约 0.1 元。

## 决策 47：F5 背得会实现（2026-09-17，用户拍板双模式制卡）

- **双模式制卡（用户补充选段模式）**：①一键生成（我的笔记列表/详情页「🎴 生成卡片」→整篇→≤15 张/批）②**选段制卡**（笔记查看页选中文字→跟随选区的浮标「为此段生成卡片」→1 张卡，答案=选中文段保留引用）。
- **数据与调度**：store cards 表（问答/来源笔记/调度字段）；backend/cards.py——SM-2 简化四键（again 当天重现 ease-0.2；hard 间隔减半；good 首次 1 天按 ease 推进；easy 1.3 倍加速）、due_queue 防积压（settings.card_daily_limit 默认 20，超限取上限、剩余顺延不惩罚）、stats/count_by_note。
- **生成**：chat.py CARD_GEN_PROMPT/CARD_TEXT_PROMPT（JSON 输出+解析容错；**踩坑：模板内 JSON 示例花括号被 str.format 当占位符→KeyError '"q"'，转义 {{ }} 修复**）。
- **端点**：POST /api/cards/generate、/api/cards/from-text、GET /api/cards/due、POST /api/cards/<id>/review、GET /api/cards/stats；settings 白名单+card_daily_limit；store 新增 get_note。
- **前端**：侧边栏「🎴 背得会」按钮+复习弹窗（cards.js：队列/翻面/四键/前置引导 toast/跳回来源笔记）；notes.js 双入口；设置页每日卡上限输入；CSS 卡片样式+选段浮标。
- **验收**：tests/test_cards.py 6/6（调度四键/防积压 no-snowball/事件落库）；E2E 实测——4 张卡生成（宁缺毋滥）、good→明天到期、again→当天重现、card_created/card_reviewed 落库。费用约 0.05 元。金标未跑（不涉及对话管线，按 S9 预算规则）。

## 决策 48：F8 考研场景包实现（2026-09-17，用户拍板含 4 项自查补漏）

- **用户自查验收标准指出 4 处漏项并全部补入**：①模式可切换（PRD 原要求「同一库可切换」，方案最初设计成建库固定——补 PATCH /api/kbs exam 字段切换）②失败降级验收（AI 分析失败跳过、面板待分析可重试）③费用闭环（分析前提示约 1~2 分/文件+F14 usage 计入 exam_tag_analyzed）④标签质量抽查（E2E 实测 6 标签全部准确）。
- **实现**：store kbs 加 exam 列（ALTER 容错）+exam_tags 表；backend/exam.py（TAG_ANALYZE_PROMPT JSON 提取 3~8 标签/set_tags 替换语义/add/delete/list/pending_files）；端点：GET/POST tags、POST tags/analyze（逐个失败静默）、DELETE tags/<id>、PATCH kbs 扩展 exam；events 白名单+exam_tag_analyzed；F14 usage 加考点分析计数。
- **前端**：建库弹窗用途单选（课程/考研）；侧边栏 kb「🎯」按钮（非考研库=confirm 设为考研；考研库=打开考点管理弹窗）；exam.js 考点面板（按文件列标签/＋手动加/×删除/AI 分析按钮带费用提示/切换回普通库）。
- **验收**：tests/test_exam.py 4/4（CRUD/pending/模式切换）；E2E——真题文件 AI 提取 6 考点全部准确、删除/切换端点通。费用约 0.02 元。金标未跑（不涉及对话管线）。踩坑：list_docs 返回 dict 列表（需取 ["rel"]）；curl 中文 JSON 400 属已知坑⑥。

## 决策 49：F9 拒答附原因实现 + F4③ 弱相关铁律落地（2026-09-17，随方案执行）

- **F9**：chat.py 新增 REFUSAL_FULL=核心句（保留「资料中未找到相关内容」开头，金标关 3 兼容）+原因+三条建议（换关键词/导入资料/生成笔记）——**规则模板、不调模型、零成本**（与 F4 验收①一致）；server chat_route 零命中输出 REFUSAL_FULL。
- **F4③ 落地（顺手项）**：qa_system.txt 铁律第 8 条——部分相关时只答相关部分并明示「仅找到部分相关内容」，禁止硬凑完整答案。
- **验收**：E2E 拒答输出=核心句+原因+建议；**金标回归 25/25 退化 0**（约 1 元，按 S9 规则对话管线变更必跑）。

## 决策 50：S12 反馈闭环与需求池 + S13 PMF 判定标准（2026-09-17，机制设计）

- **S12（docs/反馈闭环与需求池.md）**：三反馈入口盘点（意见反馈/引用错误按钮/测试观察+两个被动源）；需求池模板（ID/来源/描述/严重度/出现次数/对应假设/状态/评审结论 8 字段）；闭环三拍（收集→处理→回执——回执=GitHub Issue 回复+版本更新说明）；入库纪律（孤例观察群例评审、已拒绝保留理由）。
- **S13（docs/PMF判定标准.md）**：**标准先行**——H1~H11 全量判定表（✅/⚠️/❌ 三档）+ 北极星/推荐分/Sean Ellis 失望问题；三结论量化门槛：GO=核心假设全过+≥3 条 P1 过（含 H7 或 H10）+H8≥50%+推荐分达标；Pivot=核心部分成立但 GO 条件两项不达标（按证据转型，例：C 群全过 A 群全挂→主攻考研）；止损=核心全挂+H8<30%+推荐分低（保留作品集定位）；灰色地带禁止直接 GO（补小样本验证）。
- **S11 问卷同步**：第 11 题加「如果药鉴明天消失了你会失望吗」（Sean Ellis 40% 法则，≥40% 非常失望=PMF 信号）。

## 决策 51：v0.4.0 打包发布（2026-09-17）

- **版本**：config.VERSION=0.4.0；使用说明.txt 重写（v0.4.0 七项新功能+费用/隐私章节更新）。
- **版本控制**：主仓库 commit b512e70 → Gitee 推送成功（34480d7..b512e70）；app_public commit 43f578f → GitHub 推送成功（4e6b015..43f578f，首次两次失败 Connection reset/Could not connect，第三次成功——校园网恢复）。
- **打包**：PyInstaller onedir（约 2~5 分钟）→ zip 68.2MB（药鉴-v0.4.0-win64.zip）；打包版冒烟实测全过——health v0.4.0、usage（含考点分析计数）、cards/due（真实卡片+防积压 limit）、events/stats、backup export 200、静态资源 200、pywebview 窗口正常发起 API 调用。
- **GitHub Release v0.4.0**：创建成功（release id 390763492）+ asset 上传 68.2MB（踩坑：curl URL 中文编码丢失「药鉴-」前缀 → 资产改名 ASCII「YaoJian-v0.4.0-win64.zip」，与 v0.3.0 命名一致）。落地页 releases/latest 自动指向新版。
- **下载**：https://github.com/yuyuyuuyuyuy/yaojian/releases/download/v0.4.0/YaoJian-v0.4.0-win64.zip
